import React from 'react'
import ReactDOM from 'react-dom'
import './index.css'

const App = (params) => {
  return <div>text</div>
}

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
)
